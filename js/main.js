let carousel; // Config singleton for carousel

$(() => {
	$("body").overlayScrollbars({ // Scrollbar initialization
        "overflowBehavior": {
            "x": "hidden",
            "y": "scroll"
        }
    });
});